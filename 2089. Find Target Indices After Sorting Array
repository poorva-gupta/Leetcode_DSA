class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        nums.sort()
        stack=[]
        for i in range(len(nums)):
            if nums[i]==target:
                stack.append(i)
        return stack
        
        
