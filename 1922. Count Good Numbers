class Solution:
    def countGoodNumbers(self, n: int) -> int:
        mod=10**9+7
        even_indices=(n+1)//2
        odd_indices=(n)//2
        even=pow(5,even_indices,mod)
        odd=pow(4,odd_indices,mod)
        result=(even*odd)%mod
        return result
        
