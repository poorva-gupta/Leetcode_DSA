class Solution:
    def getCommon(self, nums1: List[int], nums2: List[int]) -> int:
        a=set(nums1)
        b=set(nums2)
        result=list(a.intersection(b))
        if not result:
            return -1
        return min(result)
        
