class Solution:
    def smallestEqual(self, nums: List[int]) -> int:
        stack=[]
        for i in range(len(nums)):
            if i%10==nums[i]:
                stack.append(nums[i])
                return i
        return -1
        
               

        
