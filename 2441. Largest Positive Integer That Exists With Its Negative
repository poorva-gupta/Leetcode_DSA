class Solution:
    def findMaxK(self, nums: list[int]) -> int:
        nums = set(nums)
        ans = -1
        for num in nums:
            if num > 0 and -num in nums and num > ans:
                ans = num
        return ans
