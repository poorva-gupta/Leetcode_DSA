class Solution:
    def getXORSum(self, arr1: List[int], arr2: List[int]) -> int:
        x = 0
        for i in range(len(arr1)):
            x = x ^ arr1[i]
        
        y = 0
        for j in range(len(arr2)):
            y = y ^ arr2[j]
        
        return x & y

        
