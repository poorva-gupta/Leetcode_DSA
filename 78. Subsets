class Solution:
    def subsets(self, nums: List[int]) -> List[List[int]]:
        a=[[]]
        for num in nums:
            n=len(a)
            for i in range(n):
                b=a[i]+[num]
                a.append(b)
        return a
        
