class Solution:
    def findMissingElements(self, nums: List[int]) -> List[int]:
        a=min(nums)
        b=max(nums)
        ans=[]
        for num in range(a,b+1):
            if num not in nums:
                ans.append(num)
        return ans
        
